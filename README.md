FirmWare Sessions :
What is firmware ? 
  - Firmware is a piece of software code that controls the hardware directly - Closely tight with the hardware level below. 
  - Firmware involved in everything from booting process to mapping any high level abstract function to hardware/register level functions.
